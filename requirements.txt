# IsoCAPE dependencies
# Install: pip install -r requirements.txt

# Core
pysam>=0.21.0
pyarrow>=12.0.0
numpy>=1.24.0
scipy>=1.10.0

# GTF parsing
gffutils>=0.12.0

# Single-cell
scanpy>=1.9.0
anndata>=0.9.0

# Parallelism
tqdm>=4.65.0

# Optional: PolyASite validation (build_matrix --polyadb)
# No additional dependencies required (uses bisect from stdlib)
